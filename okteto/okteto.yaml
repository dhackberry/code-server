build:
  code-server:
    image: okteto.dev/code-server:latest
    context: .
deploy:
  # - okteto build -t okteto.dev/code-server:${OKTETO_GIT_COMMIT} .
  - envsubst < okteto/secret.template.yaml > okteto/secret.yaml
  - kubectl apply -k ./okteto
  - kubectl apply -f okteto/manifests

